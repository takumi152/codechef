// WIP

#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

typedef long long int ll;

const ll MOD = 1000000007;

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  int T;
  cin >> T;
  vector<string> S(T);
  for (auto &x: S) cin >> x;

  for (auto &x: ans) cout << x << endl;

  return 0;
}
